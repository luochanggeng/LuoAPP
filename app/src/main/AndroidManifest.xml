<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.luo.app">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
        android:networkSecurityConfig="@xml/network_security_config">
        <!-- App总入口 -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Android TV应用, 包含Android TV开发过程中学习到的功能 -->
        <activity android:name=".tvDemo.login.LoginActivity" android:screenOrientation="landscape"/>
        <activity android:name=".tvDemo.home.HomeActivity" android:screenOrientation="landscape"/>
        <activity android:name=".tvDemo.setting.SettingActivity" android:screenOrientation="landscape"/>
        <activity android:name=".tvDemo.list.ListActivity" android:screenOrientation="landscape"/>
        <activity android:name=".tvDemo.player.FullScreenPlayerActivity" android:screenOrientation="landscape"/>
    </application>

</manifest>